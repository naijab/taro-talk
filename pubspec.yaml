name: taro_talk
description: taro talk - talk with a friend
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Firebase
  firebase_core: ^1.0.0
  cloud_firestore: ^1.0.1

  # Image
  cached_network_image: ^2.5.1
  image_picker: ^0.7.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  firebase: ">=7.0.0"

flutter:
  uses-material-design: true

  assets:
    - icons/